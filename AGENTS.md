# CYS Portfolio 작업 규칙

이 파일은 새 Codex 작업에서도 포트폴리오의 기존 디자인과 작업 맥락을 유지하기 위한 기준이다.

## 디자인 일관성

- 이 프로젝트는 기존 포트폴리오의 개선 작업이다. 새 페이지를 별도 랜딩페이지처럼 재디자인하지 않는다.
- 상세페이지의 기준은 `detail-syncflo.html`, `detail-iljin.html`, `css/detail.css`의 공통 클래스다.
- H1, H2, 본문, 섹션 라벨의 글자 계층은 반드시 기존 공통 클래스를 재사용한다.
  - Hero: `.sf-hero__kicker`, `.sf-hero__title`, `.sf-hero__desc`, `.sf-hero__tags`
  - Section: `.sf-section__label`, `.sf-section__title`, `.sf-section__body`
- 프로젝트별 `h1`, `h2`, 본문 폰트 크기·굵기·행간을 새로 선언하지 않는다. 사용자가 명시적으로 요청한 경우에만 공통 계층에서 최소한으로 조정한다.
- 프로젝트별 섹션 padding도 공통 상세페이지 범위인 약 `68–112px`을 기준으로 한다. 콘텐츠가 적은 섹션에 큰 `min-height`를 넣어 빈 공간을 만들지 않는다.
- 제목에 임의의 `<br>`를 넣어 줄바꿈을 고정하지 않는다. 문장 의미상 반드시 필요한 경우에만 사용하고, 기본적으로 컨테이너 너비와 `word-break: keep-all`에 맡긴다.
- 새 상세페이지도 공통 `<common-header>`, `<common-footer>`, `.sf-meta`, `.sf-nav`, `.snav` 구조를 사용한다.
- 보라색 대형 강조 문구, 브라우저 목업 장식, 떠 있는 상태 카드, 과한 그라데이션처럼 전형적인 AI 랜딩페이지 패턴을 추가하지 않는다.
- 프로젝트별 CSS는 콘텐츠 고유 컴포넌트와 배치에만 사용한다. 타이포그래피와 전역 내비게이션을 다시 정의하지 않는다.
- 제품 화면 캡처를 아무 연출 없이 전체 폭으로 반복 배치하지 않는다. 필요하면 HTML/CSS 브라우저 프레임, 기능 콜아웃, 화면+설명 2단 구성, 구조 다이어그램을 사용해 무엇을 봐야 하는지 먼저 설명한다.
- 화면 연출을 추가해도 실제 UI 캡처 내용을 변조하거나 구현하지 않은 기능을 만들어 넣지 않는다.

## SyncFlo AX 페이지

- 파일: `detail-syncflo-ax.html`
- 메인 Works에는 링크하지 않는 비공개 초안 페이지다. 링크가 없다고 접근이 차단되는 것은 아니다.
- `noindex`는 사용자가 명시적으로 요청할 때만 추가한다.
- SyncFlo Accounting과 같은 프로젝트군으로 보이도록 Hero, 메타 정보, 섹션 제목, Header, 하단 프로젝트 내비게이션의 톤을 맞춘다.
- 현재 화면과 범위가 확정되지 않은 WIP 프론트엔드 프로토타입이다.
- 역할 표기는 `UX/UI 디자인 · 화면 설계 · Nuxt/Vue 프론트엔드 프로토타입 구현` 범위로 제한한다.
- 실제 백엔드, MCP 서버, 온프레미스 LLM을 직접 구축했다고 과장하지 않는다.
- AI-assisted development를 사용했지만, 전달받은 요구사항을 화면 구조와 인터랙션으로 구현하고 생성 코드를 검토·수정한 것으로 설명한다.
- 사용자는 개발자나 서비스 기획자가 아니다. 포트폴리오 설명은 기술 용어보다 비용 부담 감소, 사내 자료 보호, 반복 업무 감소, 여러 사내 도구 연결처럼 누구나 이해할 수 있는 결과를 먼저 쓴다.
- RAG, MCP, LLM 같은 용어는 꼭 필요할 때만 쉬운 설명을 함께 붙이고, 화면 본문에서는 `사내 자료 참고`, `업무 도구 연결`, `회사 내부 AI`처럼 풀어서 표현한다.

## Git과 배포

- `.claude/settings.local.json`은 사용자 변경이므로 수정하거나 커밋하지 않는다.
- 사용자가 요청하기 전에는 Git 커밋, push, Vercel 배포를 하지 않는다.
- 배포 전 `git diff --check`와 변경 파일 목록을 확인한다.
- 실제 서비스 주소는 `https://cys-design.vercel.app/`이다.

## 작업 원칙

- 기존 기능과 사용자 변경을 보존한다.
- 디자인 수정 전 관련 공통 스타일과 유사 상세페이지를 먼저 확인한다.
- 한 페이지를 개선할 때 다른 상세페이지의 공통 스타일에 예상치 못한 영향이 없는지 선택자 범위를 확인한다.
- 새 컴포넌트를 만들기 전에 기존 클래스와 구조로 해결할 수 있는지 먼저 확인한다.
